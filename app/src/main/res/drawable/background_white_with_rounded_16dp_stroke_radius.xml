<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 내부를 채우는 색상 -->
    <solid android:color="@color/edit_profile_button_solid" />
    <!-- 외곽선의 두께와 색상 -->
    <stroke
        android:width="2dp"
        android:color="@color/recommendation_stroke" />
    <!-- 외곽선의 모서리를 둥글게 함 -->
    <corners android:radius="16dp" />
</shape>